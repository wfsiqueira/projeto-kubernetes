apiVersion: v1
kind: Service
metadata:
  name: vmware-exporter
  namespace: monitoring
spec:
  selector:
    app: vmware-exporter
  ports:
    - protocol: TCP
      port: 9272
      targetPort: 9272
  type: LoadBalancer